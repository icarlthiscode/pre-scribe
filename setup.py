from setuptools import setup

setup(
    name = 'pre-scribe',
    version = '0.0.1',
    description = 'A lightweight server-cached search index generator.',
    packages = ['prescribe'],
    package_dir = {'' : 'src'},
    install_requires = [
    ]
)
